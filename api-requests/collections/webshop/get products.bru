meta {
  name: get products
  type: http
  seq: 2
}

get {
  url: {{product_base_URL}}/products
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  : eq 
}

settings {
  encodeUrl: true
  timeout: 0
}
